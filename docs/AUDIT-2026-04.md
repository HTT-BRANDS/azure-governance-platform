# Production Audit — April 2026 (scaffold)

> **This file is regenerated by `scripts/audit_production.py`.** The content
> below is a placeholder so the GitHub Pages render has something to show
> before the first audit run. Do not hand-edit — your changes will be
> overwritten on the next `python scripts/audit_production.py --env staging`.

Environment: **unknown (awaiting first run)**
Tenants checked: **0 (awaiting first run)**

## Consent Gaps
_Awaiting first audit run._

## RBAC Gaps
_Awaiting first audit run._

## Sync Errors & Data Freshness
_Awaiting first audit run._

## UI-Fixture Leaks
_Awaiting first audit run._

---

## How to run the audit

```bash
# Locally (uses OIDC dev fallback or az login):
OIDC_ALLOW_DEV_FALLBACK=true python scripts/audit_production.py --env staging

# In CI (azure/login@v2 already done in the workflow):
python scripts/audit_production.py --env production
```

Outputs:
- `scripts/audit_output.json` — machine-readable report consumed by Pages (`scripts/render_status.py`).
- `docs/AUDIT-YYYY-MM.md` — this file, overwritten monthly.
